import os
import logging

logger = logging.getLogger(__name__)

SECRET_KEY = os.getenv("SECRET_KEY")
if not SECRET_KEY:
    SECRET_KEY = "dev-secret-key-change-in-production"
    # ---------------------------------------------------------------
    # ПУНКТ 8: Предупреждение в лог, если используется дефолтный ключ.
    # Раньше это происходило молча — можно было случайно задеплоить
    # на прод с небезопасным ключом и не заметить.
    # Теперь при каждом старте в консоль пишется явное предупреждение.
    # ---------------------------------------------------------------
    logger.warning(
        "\n"
        "==============================================================\n"
        "  ВНИМАНИЕ: SECRET_KEY не задан!\n"
        "  Используется небезопасный ключ по умолчанию.\n"
        "  Перед деплоем установите переменную окружения:\n"
        "    export SECRET_KEY='ваш-случайный-секретный-ключ'\n"
        "  Сгенерировать ключ: python -c \"import secrets; print(secrets.token_hex(32))\"\n"
        "=============================================================="
    )

ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 60
